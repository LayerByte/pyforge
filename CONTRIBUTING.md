# Contributing

Use Python 3.12 or newer. Create a focused branch, add tests for behavior
changes, run `pytest` and `ruff check .`, and use Conventional Commits.
Keep security features defensive and never add live credentials to fixtures.
