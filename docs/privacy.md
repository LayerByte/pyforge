# Privacy

PyForge operates locally unless the user explicitly invokes a network tool.
History stores only the tool name, timestamp, outcome, and duration. It never
stores tool arguments, passwords, tokens, secrets, file contents, or response
bodies. Environment values with sensitive names are redacted before display.
